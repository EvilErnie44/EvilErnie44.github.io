import {Dice} from "./dicemodel";
import {Display} from "./diceview";

/*

let ukDice = new Dice(99);
 let viewModel = viewInterface;
 viewModel.max = ukDice.max;
 viewModel.min = ukDice.min;
 viewModel.rand = ukDice.roll();
 let output = new Display();
 output.update();

 */
